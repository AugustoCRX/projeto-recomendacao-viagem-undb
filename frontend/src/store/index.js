// Global state management.
// Will be expanded with Zustand (or Context) slices as features grow.
// Each feature can also have its own local store slice imported here.

// Example slices to be added:
// - tripsStore  → list of trips, selected trip
// - uiStore     → sidebar open/close, active modal, loading states
// - mapStore    → map viewport, selected markers
